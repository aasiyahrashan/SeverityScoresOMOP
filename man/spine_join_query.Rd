% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/construct_with_statement.R
\name{spine_join_query}
\alias{spine_join_query}
\title{Build LEFT JOINs from the spine to each table CTE.
Each join is on plain columns (no COALESCE), so the DB can use indexes.}
\usage{
spine_join_query(aliases)
}
\arguments{
\item{aliases}{Character vector of CTE alias names.}
}
\value{
A SQL string with FROM spine + LEFT JOIN clauses.
}
\description{
Build LEFT JOINs from the spine to each table CTE.
Each join is on plain columns (no COALESCE), so the DB can use indexes.
}
