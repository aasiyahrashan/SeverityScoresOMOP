% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ground_truth_functions.R
\name{validate_ground_truth_vs_omop}
\alias{validate_ground_truth_vs_omop}
\title{Check overlap between ground truth patients and OMOP patients.}
\usage{
validate_ground_truth_vs_omop(
  conn,
  gt_config,
  schema,
  start_date,
  end_date,
  dialect
)
}
\arguments{
\item{conn}{A database connection object.}

\item{gt_config}{A \code{gt_config} object created by \code{ground_truth_config}.}

\item{schema}{The OMOP schema name.}

\item{start_date}{Start date filter for OMOP ICU stays (character, inclusive).}

\item{end_date}{End date filter for OMOP ICU stays (character, inclusive).}

\item{dialect}{A SQL dialect supported by SqlRender.}
}
\value{
A list with two elements:
\describe{
  \item{gt_missing_from_omop}{Vector of ground truth patient keys not found in OMOP.}
  \item{omop_missing_from_gt}{Dataframe of OMOP person_ids with ICU stays not found in ground truth.}
}
}
\description{
Performs a bidirectional check:
\enumerate{
  \item Which ground truth patients are missing from OMOP (via the joining schema)?
  \item Which OMOP ICU patients (from visit_detail) are missing from ground truth?
}
}
\details{
Prints warnings summarising mismatches and returns a list with the details.
}
