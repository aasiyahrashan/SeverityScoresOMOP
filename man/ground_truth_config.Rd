% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ground_truth_functions.R
\name{ground_truth_config}
\alias{ground_truth_config}
\title{Create a ground truth configuration object.}
\usage{
ground_truth_config(
  df,
  joining_schema,
  joining_person_table,
  joining_visit_table,
  gt_person_key,
  omop_person_key,
  gt_encounter_key,
  omop_encounter_key,
  gt_icu_admission_col,
  gt_icu_discharge_col,
  gt_hospital_admission_col = NULL,
  gt_hospital_discharge_col = NULL
)
}
\arguments{
\item{df}{A dataframe containing ground truth ICU stay data.}

\item{joining_schema}{Schema name in the OMOP database containing ID mapping tables.}

\item{joining_person_table}{Table name in \code{joining_schema} that maps external
patient identifiers to OMOP \code{person_id}.}

\item{joining_visit_table}{Table name in \code{joining_schema} that maps external
encounter identifiers to OMOP \code{visit_occurrence_id}.}

\item{gt_person_key}{Column name in \code{df} for the patient identifier
(e.g. \code{"PatientDurableKey"}).}

\item{omop_person_key}{Column name in \code{joining_person_table} that matches
\code{gt_person_key} (e.g. \code{"PatientDurableKey"}).}

\item{gt_encounter_key}{Column name in \code{df} for the encounter identifier
(e.g. \code{"EncounterKey"}).}

\item{omop_encounter_key}{Column name in \code{joining_visit_table} that matches
\code{gt_encounter_key} (e.g. \code{"EncounterKey"}).}

\item{gt_icu_admission_col}{Column name in \code{df} for ICU admission datetime.}

\item{gt_icu_discharge_col}{Column name in \code{df} for ICU discharge datetime.}

\item{gt_hospital_admission_col}{Optional. Column name in \code{df} for hospital
admission datetime. If NULL (default), hospital times are taken from OMOP
visit_occurrence instead.}

\item{gt_hospital_discharge_col}{Optional. Column name in \code{df} for hospital
discharge datetime. If NULL (default), hospital times are taken from OMOP
visit_occurrence instead.}
}
\value{
A list of class \code{"gt_config"} containing all parameters.
}
\description{
Bundles all ground-truth-related parameters into a single object
for cleaner function signatures. Pass the result to
\code{get_score_variables(ground_truth = ...)}.
}
\examples{
\dontrun{
gt <- ground_truth_config(
  df = read.csv("ground_truth.csv"),
  joining_schema = "clarity_omop_map",
  joining_person_table = "person_map",
  joining_visit_table = "visit_map",
  gt_person_key = "PatientDurableKey",
  omop_person_key = "PatientDurableKey",
  gt_encounter_key = "EncounterKey",
  omop_encounter_key = "EncounterKey",
  gt_icu_admission_col = "CrossIcuStayStartInstant",
  gt_icu_discharge_col = "CrossIcuStayEndInstant"
)

data <- get_score_variables(..., visit_mode = "ground_truth", ground_truth = gt)
}

}
