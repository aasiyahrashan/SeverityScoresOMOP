% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ground_truth_functions.R
\name{resolve_ground_truth_ids}
\alias{resolve_ground_truth_ids}
\title{Resolve ground truth to OMOP IDs.}
\usage{
resolve_ground_truth_ids(conn, gt_config, dialect)
}
\arguments{
\item{conn}{A database connection object.}

\item{gt_config}{A \code{gt_config} object created by \code{ground_truth_config}.}

\item{dialect}{A SQL dialect supported by SqlRender.}
}
\value{
The ground truth dataframe with \code{person_id},
  \code{visit_occurrence_id}, and \code{icu_admission_datetime} /
  \code{icu_discharge_datetime} (as formatted character strings) added.
  All original columns are preserved.
}
\description{
Queries the joining schema to map external patient and encounter keys
to OMOP person_id and visit_occurrence_id. Returns the full ground truth
dataframe with resolved IDs attached. Rows that cannot be resolved are
dropped with a warning.
}
