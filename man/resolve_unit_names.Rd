% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_query_function.R
\name{resolve_unit_names}
\alias{resolve_unit_names}
\title{Replace unit_concept_id integers in wide pivoted result with concept names.}
\usage{
resolve_unit_names(pivoted, conn, schema, dialect)
}
\arguments{
\item{pivoted}{data.table output from pivot_long_to_wide().}

\item{conn}{DBI connection.}

\item{schema}{OMOP schema name.}

\item{dialect}{SQL dialect.}
}
\value{
The same data.table with unit_ columns replaced by concept names.
}
\description{
pivot_long_to_wide() produces unit_{short_name} columns containing raw
unit_concept_id integers. This function queries the concept table once for
all distinct IDs present, then replaces the integers with concept names.
Operating on the pivoted result (~104k rows, ~20 distinct IDs) rather than
the filtered temp table (21.5M rows) avoids a LEFT JOIN on every raw row.
}
\keyword{internal}
