% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/construct_with_statement.R
\name{spine_query}
\alias{spine_query}
\title{Build a UNION spine of all (person_id, icu_admission_datetime, time_in_icu)
from every table CTE. Replaces the old FULL JOIN chain with COALESCE keys.
UNION (not UNION ALL) deduplicates, so a patient-day present in multiple
tables appears once in the spine.}
\usage{
spine_query(aliases)
}
\arguments{
\item{aliases}{Character vector of CTE alias names (e.g. c("m", "o", "drg"))}
}
\value{
A SQL string defining the spine CTE.
}
\description{
Build a UNION spine of all (person_id, icu_admission_datetime, time_in_icu)
from every table CTE. Replaces the old FULL JOIN chain with COALESCE keys.
UNION (not UNION ALL) deduplicates, so a patient-day present in multiple
tables appears once in the spine.
}
