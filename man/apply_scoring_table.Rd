% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring_functions.R
\name{apply_scoring_table}
\alias{apply_scoring_table}
\title{Apply scoring ranges from a lookup table to physiology variables.}
\usage{
apply_scoring_table(data, scoring_path, imputation = "normal")
}
\arguments{
\item{data}{A data.table with physiology variables.}

\item{scoring_path}{Path to the scoring CSV (e.g. apache_ii_scoring.csv).}

\item{imputation}{Either "normal" (missing = 0 points) or "none" (missing = NA).}
}
\value{
The data.table with subscore columns added (e.g. min_temp_score, max_temp_score).
}
\description{
Reads a scoring CSV and assigns points based on value ranges. Handles
both min and max columns, and the "worst score" logic (pmax of min/max).
Does NOT handle special cases (AaDO2, GCS, creatinine doubling, chronic health,
bicarbonate/pH fallback, mechanical ventilation) — those stay in the
score-specific functions.
}
