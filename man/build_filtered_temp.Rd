% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/construct_with_statement.R
\name{build_filtered_temp}
\alias{build_filtered_temp}
\title{Build statements to create a filtered temp table for one OMOP table.}
\usage{
build_filtered_temp(
  table_concepts,
  table_name,
  variable_names,
  has_string_ids = FALSE
)
}
\arguments{
\item{table_concepts}{Concepts dataframe, pre-filtered to this table.}

\item{table_name}{OMOP table name.}

\item{variable_names}{Variable names dataframe.}

\item{has_string_ids}{Logical. If TRUE, string_resolved_ids temp table
exists and this table has entries in it.}
}
\value{
Character vector: DROP, CREATE TEMP TABLE, ANALYZE statements.
}
\description{
Filters the clinical table by person_batch (IN subquery) and concept IDs.
Pre-joins unit names (LEFT JOIN concept on unit_concept_id) for tables
with numeric variables.
}
\details{
When multiple filter types exist (direct IDs, ancestor subquery,
string_resolved_ids), uses UNION instead of OR. This prevents
Postgres from falling back to sequential scans when it can't optimise
an OR across different filter strategies (e.g. IN-list vs subquery).
}
\keyword{internal}
